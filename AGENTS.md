# AGENTS.md — Instructions for OpenCode Sessions

This file is for AI coding assistants (OpenCode). It provides architecture context, coding rules, and session state so new sessions can resume work immediately without reading the full codebase.

---

## Project Identity

- **Name:** Sitara ERP (Node.js SaaS port)
- **Path:** `D:\SasS`
- **Original PHP app:** `C:\xampp\htdocs\sitara`
- **Goal:** Multi-tenant SaaS ERP for Pakistani SMBs (originally purses/bags retail)
- **Stack:** Express.js + TypeScript + Prisma + PostgreSQL 16 + Next.js 14 + Tailwind CSS + shadcn/ui

---

## Project Structure

```
D:\SasS/
├── apps/api/src/
│   ├── routes/           # Express route files (18 files, one per module)
│   ├── services/         # Business logic (9 services)
│   ├── middleware/        # auth, tenant, rbac, rateLimit, validate, errorHandler
│   ├── lib/              # prisma.ts, redis.ts, queue.ts
│   ├── config/           # index.ts (env-based config)
│   └── constants/        # accounts.ts (ACCOUNT_CODES)
├── apps/web/src/app/     # Next.js 14 App Router pages (40+ pages)
└── apps/api/prisma/      # schema.prisma (41 models), seeds
```

**Docker:** PostgreSQL on port 5435, Redis on port 6380

---

## CRITICAL — Architecture Rules

### 1. No Hardcoded IDs
- `createdBy` must always use `req.user ? BigInt(req.user.userId) : throw`
- `warehouseId` must use `getDefaultWarehouse(tenantId, tx)` from `utils/warehouse.ts`
- Never use literal `1` for any entity ID (customer, product, warehouse, user)

### 2. No Hardcoded Account Codes
- All account code strings in business logic must use `ACCOUNT_CODES` from `constants/accounts.ts`
- Account ID lookup: `chartOfAccount.findUnique({ where: { tenantId_accountCode: ... } })`
- Supported codes: `SALES_REVENUE (4000)`, `SALES_RETURNS (4100)`, `ACCOUNTS_PAYABLE (2000)`,

### 3. All Multi-Step DB Ops in Transactions
- Use `prisma.$transaction(async (tx) => { ... })` for any operation that touches >1 table
- This includes: checkout, purchase receiving, expense approval, sale cancellation

### 4. Journal Entries Must Balance
- Always compute `totalDebit` and `totalCredit` and verify equality before committing
- Throw with message if unbalanced: `throw new Error('Journal entry not balanced: Dr X != Cr Y')`
- Account lookup must be inside the transaction (`tx.chartOfAccount.findUnique`)

### 5. FIFO Stock — Consumption Order
- **Sale (consume):** oldest batches first (`receivedAt ASC`, `quantityRemaining > 0`)  
- **Return (restore):** most recent batch first (`receivedAt DESC`, no `gt: 0` filter)  
- Always decrement both `warehouse_stock` AND `stockBatch.quantityRemaining` in sync
- Validate stock availability before creating the sale record

---

## Current Session State

### Last Session Date: 2026-09-19
### C-A2b restore matrix: COMPLETE (all 10 tests + 2 gap tests PASS, prod clean)
- Commit `bd6845c` (tenant-scoped transactional restore) + `32f15fb` (junction deletes via parent scope) deployed; health `ok`
- T1 happy-path, T2 adversarial child-first order, T3 self-lockout+login, T4 sequence health, T5 mid-restore fail-loud + byte-identical rollback, T6 no-swallow grep, T7/T8 pre-flight gates, T9 record-scope + payload `Backup tenant mismatch`, T10 auto-snapshot discoverable
- Prod verified clean: `backup_records=0`, no `p2rst` tenants/users, `/storage/backups/` empty, tenant 1 untouched, PM2 all online
- Test-harness lessons: use `sql1()` sanitizer (` | grep -E '^[0-9]+$'`) for INSERT...RETURNING captures (psql prints `INSERT 0 1` tag); service API is `authService.registerTenant(name,slug,email,pass,adminName)` positional + free fns `createBackup/requestRestoreToken/executeRestore`; pass IDs via `process.argv` with fully-quoted heredocs; wrap every `npx tsx` in `timeout 150`
- Unexplained single `t3login` tsx hang (epoll, 17 min, killed); login path verified functional before/after — treated as transient, timeout guards now standard
- Redis scare resolved: `-u` URL-parse quirk only; `-a` PONG ok, 0 app connection errors — no outage, no rotation issue
- Process lesson: hotfix was committed before diff review — standing rule is diff-before-COMMIT, not just diff-before-deploy; tighten on next hotfix
- `ca2b-matrix.sh` T5 uses `tail -n 4` which truncates Prisma multi-line errors before the `T5ERR=` marker (false FAIL; re-proven via Gap B) — fix truncation before trusting a clean run if restore code is touched again
- Backup payload shape: `payload.data[modelName] = Array` (dict keyed by model, NOT a list of `{model, rows}`)

### C-B1 middleware hardening + CORS: COMPLETE (commit `edb33a3`, deployed, 19/19 tests PASS)
- `tenant.ts`: port-strip (incl. IPv6-bracket edge), lowercase, `TENANT_SLUG_PATTERN` + `isReservedTenantSlug` (37) replace hardcoded 4-list, explicit `!req.user` 401 guard (defense-in-depth; `{...undefined}` never crashed, it made malformed shape)
- `index.ts`: static 2-origin allowlist → validated `https://*.sitarapurse.com` reflection (leading-dot suffix + LDH label check); no-Origin → allow; dev `*` untouched; apex correctly rejected
- Nested `a.b` hosts resolve by first label by design (DNS wildcard routes single-level only); unknown labels 404 at lookup layer, never CORS layer
- Deploy notes: `deploy-sitara.ps1` preflight has a Windows→ssh quoting bug (`grep -E "sitara-(...)"` parens) — deploy by running its steps via base64-wrapped remote script; VPS `M package-lock.json` was npm `"peer": true` churn, safe to `git checkout --` before pull
- Deploy-script fix COMPLETE: `deploy-sitara.ps1` preflight now via `Invoke-RemoteScript` (base64; single-quoted concat so `$` survives) + health-gate pattern fixed `status....ok`→`status...ok` (4 dots never matched the body; 3 dots do). Mangler characterized: old path strips ONLY `"` chars. CAUTION: historical `HEALTH_OK` lines predate this fix and came via ad-hoc workaround scripts, not this file — do not cite them as proof this script worked on date X. `.sh` counterpart runs on-box natively, never had the bug.
- FAST-FOLLOW next time `deploy-sitara.ps1` is touched: health gate uses `status...ok` (3-dot, coincidental match on current JSON shape) — swap for quoted-literal `'"status":"ok"'` or a real JSON parse (`jq`/python); the base64 transport now carries quotes safely, so the original reason for avoiding it is gone.
- Build #2 carry-over: Apache `ServerAlias *.sitarapurse.com` is a glob and MATCHES multi-level (`a.b...`) unlike DNS wildcards — after vhost goes live, probe a multi-level Host explicitly and confirm first-label behavior is acceptable; do not assume DNS single-level semantics transfer to Apache
- Build #2 status (2026-09-19): S1 `.cloudflare.ini` 600 + CRLF fix, S2 wildcard A (id `9049cc30...`, 14 records, SSL `full`), S3 `sitarapurse-wildcard` cert (serial `0529AA...`, exp 2026-12-18, app cert untouched), S4 wildcard vhost live + rollback-tested (default unchanged, app/api keep original cert), S5 E2E green (prod HTTP registration is DISABLED — fixtures via service only; join table is `role_user` singular)
- TODO to close Build #2: S6 token rotation (verify `renew --dry-run` on NEW token before revoking old) + Apache guard rejecting multi-label Hosts in wildcard vhost (anti-phishing hygiene, non-blocking)
- Build #2 CLOSED 2026-09-19: S6 rotation done (new token placed via stdin-pipe, `renew --dry-run` green BEFORE old revoked; local↔live SHA256 match, `.old-backup` removed, post-rotation api/web/wildcard all 200). Remaining non-blocking follow-up: Apache multi-label Host guard in wildcard vhost.
- NOTE: prod HTTP registration is DISABLED (`403 Public registration is disabled`) — S5 fixtures went via service layer; if self-registration is ever enabled, re-verify reserved-slug rejection + subdomain E2E through the real HTTP endpoint before flipping the flag (untested path)

### Log-on-500 class-(c) closeout: COMMITTED as `841764c`, deployed [UNVERIFIED-vs-git: test/session claims below from harness output, not in git]
- Reconstructed (inference, not on record anywhere): catches returning 200-with-fallback that B1-B4 couldn't mechanically fix; zero unlogged-500 sites found by leg-split grep [UNVERIFIED-vs-git: session grep output only, not in git]
- Phase 1: 82 added lines containing logger.warn, 0 removed (841764c: 98 ins / 79 del over 28 files) - net-new call sites [git-verifiable]; responses byte-identical [UNVERIFIED-vs-git]; (a): backup config lie → honest 500, retention cleanup → warn+continue, export per-model skips → fail-loud (C-A2b-compatible: createBackup marks failed+rethrows, snapshot failure aborts pre-transaction); (c): intent comments on fail-closed/permit-default swallows
- Harness lessons: `wcount` via `grep -c || echo 0` double-prints (`grep -c` already prints 0) — use capture-then-default; `tail -n 4` truncates Prisma errors (known); tsx-process logger output goes to stdout, NOT the PM2 log file (grep the harness output, not /var/log); join table is `role_user` singular; throwaway-tenant cleanup order documented in cc-tests runs
- Prod HTTP registration disabled noted above; test-4 failure branch + print-receipt route catches verified by code (unreachable live without DB fault — router.param guards 400 malformed ids, NaN query params absorbed)
- ASTERISK carried forward: A1 (`backup.routes.ts:117` honest-500 fix) verified by code-review/analogy only — its failure branch was never observed firing live. If that endpoint is touched again, re-prove it; do not cite this pass as proof.

### Validate-manager hardening + multi-role UI: COMPLETE (commit `5f8739a`, deployed [UNVERIFIED-vs-git: commit touches 3 code files only; deploy/health/tests from session output])
- validate-manager was NEVER unauthenticated (mount-level auth+tenant in `routes/index.ts:45`); audit flag was declaration-level only. Real fixes: login-grade limiter (5/15min), 403→401 oracle close, wrong-password increments shared loginAttempts. Checkout re-verifies creds independently — endpoint grants no capability alone
- Roles: schema is many-to-many (`role_user`); UI was single-select + hardcoded admin-filter + first-role-only preselect (editing multi-role user silently stripped extras). Now checkbox multi-select, all roles listed, admin locked-on for last-admin via existing `isLastAdmin`. No prod incident ever occurred (0 multi-role users, 0 role-change history)
- Harness note: tripping the `auth:*` limiter from localhost only affects server-originated bursts [SUPERSEDED: was "real users key by their own IP" — through Cloudflare, req.ip is the edge address (observed `auth:172.68.x.x`); see follow-up (3)]; bursts show mixed 401/429 sequencing — throttle engages, exact pattern has an unexplained nuance, no security impact
- Open question if `rateLimit.ts` is ever revisited: confirm fixed-vs-sliding window algorithm to explain non-monotonic 401/429 interleaving under burst (request passing just past a window boundary would explain it; security conclusion holds either way)

### Route-UI orphan audit: COMPLETE (read-only) + expense modal BUILT (commit `c2950c8`, deployed [UNVERIFIED-vs-git: commit touches expenses/page.tsx only; tests from session output])
- 37 orphan backend routes catalogued [UNVERIFIED-vs-git: audit table from session grep output, not in git]; audit grep-pattern lesson: raw `fetch()` + generic `handleSave(category)` indirection evade literal `apiPost('...')` searches — `PUT /settings/hardware` + `POST /settings/logos/:slot` were FALSE orphans (both wired), caught on re-check before building. Always re-verify orphans for indirect call sites before acting
- Expense `POST /` was the one true trivial gap (dead button, 30-line page) → create modal following categories-page pattern; live-tested (validation 400 parity, 201+persist+list) [UNVERIFIED-vs-git: test output from session only]. PUT/DELETE expense endpoints remain orphaned by scope decision

### Moderate tier Item 1: UI-proven a–d; permission-gate evidence per step 4 (commit `1e4d8ca`, deployed)
- `vendors/[id]/edit` did NOT exist while TWO links pointed at it (detail button + list row action) — dead links, fixed by creating the page (customers-edit mirror); no entry-point change needed. UI evidence via Playwright (`D:\SasS-uitest\`, outside repo): first run 27 PASS lines counted from output (that script had no tally), gate run PASSED 15/15 — create-via-UI → row-Edit click → 8-field prefill → save (PUT body + toast captured) → redirect → reopen match → not-found states with zero PUTs → view-only user: form renders, submit → 403 toast, URL stays, row untouched → no-view user sees no Edit affordances; screenshots in uitest/shots
- Verified before writing: no shared form, PUT passes body straight through (`vendorService.update` 68-73, no whitelist — REPORTED gap, unfixed), `GET /:id` returns `mapVendor` shape for prefill
- `''`-vs-NULL proven live with IS NULL/= '' checks (NOT bare psql print): create stores NULL (`|| null` code), edit stores `''` (customers-edit convention). No unique on optional Vendor cols (only `@@unique([tenantId, code])`) so harmless here
- PUT returns 200 `Updated` on zero-row match — proven live with TWO throwaway tenants (B PUT on A's vendor: md5 + updated_at identical after). Edit page can't hit it (GET guard precedes PUT)
- Gap #1 severity PROVEN live (A4 probe, throwaway tenant): PUT `{"currentBalance":12345,"isActive":false}` applied both — balance overwritten with no ledger entry, vendor deactivated. Priority follow-up; Item 1 doesn't worsen it (UI sends 8 fields only)
- Save-button gate: customers-edit does NOT gate submit (hasPermission only on credit-limit/pricing fields) — vendors page matches reference, no diff needed. Record corrected (was claimed gated)
- Item 1.5 mechanical sweep DONE (redone TWICE: first pass missed single-quoted object literals, second added backtick nav forms — nav 20→29): 92 routes, 152 extracted targets, 84 unique, 83 matched, 1 unmatched (forms lit=41 obj=59 tpl=23 nav=29). Sole dead static link: `sales/page.tsx:38` → `/sales/returns/create/:id` (page reads `?saleId=`; Item 1.6 deployed: link e1088ed; its [saleId] effect refired per keystroke, fixed forward f4847f2; browser evidence: ui16.js PASSED 15/15 (typing fires 0 requests, arrival fires exactly 1 GET, editing after arrival no refire, bad ids toast, old link 404, no 429); direct load of ?saleId=22 showed sale number + items with 1 GET; click-path preload NOT observed (ui16b 12/14: sale-number-visible and line-items-visible FAILED right after click, isVisible without wait, suspected timing, not rerun); HELD row has no Return link. Implemented as link change + mount-only `useEffect`). Backend-generated links (search `link:`, notification `link:`) all resolve. Query-param deep-links (`/expenses?status=`, `/sales/returns?status=`) land on pages that IGNORE the param (client-side filter only) — cosmetic wrinkle, noted
- UI-test lesson: browser runs share egress IPs — 4 logins in ~10 min tripped the `auth:*` 5/15min limiter mid-run (session died → login page). Budget ≤3 logins per 15-min window per run, or expect 429s; a 10-min wait clears it. Session-death mechanism (code-read, REPORTED unfixed): throttled `/auth/refresh` → `!res.ok` → `clearTokens()` → `logout()` redirect — `lib/api.ts` has no 429 branch. Screenshots + request log + console capture per step proved invaluable (caught strict-mode, label-binding, and session-death issues visibly)
- REPORTED-unfixed follow-ups (priority order): (1) vendor PUT mass assignment — OBSERVED live (`currentBalance`+`isActive` overwritten, no ledger) — pre-go-live blocker; (2) PUT 200-on-zero-row-match — OBSERVED live (md5-identical); (3) login limiter 5/15min keyed auth:<req.ip>; general limiter 30/min keyed ratelimit:<req.ip> (rateLimit.ts:16-22, :27-29, :71-75; index.ts:42 sets trust proxy 'loopback' [CODE-VERIFIED]. index.ts:92 global mount); OBSERVED live (2026-09-20): after one login attempt Redis held auth:172.68.x.x and ratelimit:172.68.x.x (Cloudflare edge range, PTTL ~897 s), so req.ip is the EDGE address, not the end client. INFERRED, NOT MEASURED: whether unrelated users behind one edge address share buckets. Traffic measured 2026-09-20 (one user, navigations paced 25 s apart): 3-5 API calls per page, peak 13 requests in 60 s, zero 429s; POS scanning and fast clicking NOT measured. PRE-GO-LIVE BLOCKER next to mass assignment. Fix directions (own item, report-only): trust CF ranges / CF-Connecting-IP allow-list, key authed routes by user/tenant, revisit 30/min, add 429 branch to lib/api; (4) lib/api.ts half CODE-VERIFIED (refresh !res.ok -> clearTokens; 401 path -> logout(); no 429 branch); /auth/refresh has no route-level limiter (auth.routes.ts:118), only the 30/min global (index.ts:92) applies [CODE-VERIFIED]; whether it trips in practice: NOT MEASURED. (5) vendors: OBSERVED live; customers-edit: CODE-VERIFIED (inferred from lines 75-100, ungated submit), not observed; (6) sales list Return link and New Return button render without sales.returns.create while backend POST /sales-returns requires it (sales-returns.routes.ts:152) - same class as (5); (7) approve reuses the create slug for purchases.returns (purchase.routes.ts:260 create / :360 approve) and inventory.adjustments (inventory.routes.ts:70 create / :104 approve), unlike sales.returns.approve - segregation-of-duties gap; (8) return-logs.routes.ts:9,21,33 have no route-level rbac and the mount (routes/index.ts:46) adds only authMiddleware + tenantMiddleware [CODE-VERIFIED]: any authenticated user of a tenant can read return logs; Item 6 must add a gate (confirm an existing slug, do not invent one); (9) settings.routes.ts registers GET/PUT /:category before the specific paths - ordering hazard, fall-through behavior not yet read; (10) inventory.routes.ts:104-125 PATCH /adjustments/:id/approve has no already-approved guard (approvedAt is never checked, so a second approve applies the stock change again) and DECREASE has no floor check [CODE-VERIFIED]; (11) POST /pos/checkout returns HTTP 500 for the business error Insufficient stock [OBSERVED live 2026-09-20, throwaway tenant]; (12) malformed JSON bodies return a generic 500 (POST /auth/refresh with body {bad returned 500 An unexpected error occurred) [OBSERVED live 2026-09-20]; suspected cause: error handler maps body-parser errors to 500 [INFERRED]; (13) loans: PUT /loans/:id (loan.routes.ts:117-127) whitelists only notes and status, but status is a free string with no enum or state check; POST /loans/:id/payments recomputes status from the remaining amount on every payment and checks neither amount <= remaining nor loan status; PUT on a nonexistent id returns 200 [CODE-VERIFIED]; (14) loans create (loan.routes.ts:75-115): type is upper-cased for storage but the party and journal branches compare the raw type, so a lowercase type creates a loan with no party and no journal entry; loan numbers use 4 random digits under a unique constraint (collisions return 500); journal entries are silently skipped when the cash or loan accounts are missing [CODE-VERIFIED]; (15) loans payments: remainingAmount is clamped at 0 (loan.routes.ts:141) but the journal entry books the full payAmount (:151-152, :158-159), so an overpayment credits Loans Receivable (or debits Loans Payable) beyond the subledger balance; entries stay balanced [CODE-VERIFIED]; (16) loans UI: the New Loan button and Record Payment are shown without loans.manage (a view-only user sees both; the backend would 403) [OBSERVED live 2026-09-20, throwaway tenant, ui2_viewer.js INFO counts 1 and 1]; (17) POST /loans destructures notes but never stores it (loan.routes.ts:75-115), so notes can only be set through the Item 2 dialog [CODE-VERIFIED]; (18) loan route handlers return error.message in the 500 body (loan.routes.ts:126, :165), which can expose Prisma details [CODE-VERIFIED; OBSERVED live 2026-09-20: PUT /loans/:id with an object-valued notes returned HTTP 500 whose body contains the Prisma invocation text (method updateMany, where clause with id and tenantId, argument names)]; (19) the loans/[id] status badge map lists PARTIALLY_PAID and OVERDUE, which no backend path sets (only ACTIVE and PAID) [CODE-VERIFIED]; (20) PUT /loans/:id {"status":"PAID"} on an ACTIVE loan with zero payments sets status PAID while remaining stays 1000.00 [OBSERVED live 2026-09-20]; (21) expenses: PATCH /expenses/:id/approve (expense.routes.ts:173-182) and /pay (:184-224) have no status precondition and no already-approved or already-paid guard, so any status can be moved to APPROVED or PAID (PENDING to PAID skips approval, PAID back to APPROVED, CANCELLED to PAID); approve, PUT and DELETE on a nonexistent id return 200, pay returns 404; the creator can approve and pay their own expense (no createdBy comparison) [CODE-VERIFIED]; (22) expenses pay is not atomic: the status update (:189-192) runs before the journal create (:206-219) with no transaction, a missing cash or expense account silently skips the journal, and a journal failure returns 500 after the expense is already PAID (AGENTS.md rule 3 says multi-step operations use a transaction) [CODE-VERIFIED]; (23) expenses pay chooses the debit account by accountName containing the category name (case-insensitive, no accountType filter, :198-200) and falls back to the first EXPENSE account (:201-203): a category whose name matches a non-expense account would debit that account [CODE-VERIFIED; live probe in the Item 3 round]; (24) expenses PUT (:146-163) is whitelisted (categoryId, amount, description, expenseDate) but has no status check and no amount validation, so APPROVED, PAID and CANCELLED rows are editable; DELETE (:165-171) sets CANCELLED on any status including PAID and does not reverse the PAY journal [CODE-VERIFIED]; (25) expenses POST (:100-124) destructures paymentMethod, referenceNumber and notes but stores none of them (the UI sends paymentMethod); the CSV export column Payment Method is filled from vendorName (:134-137); the list (:87-98) has no status filter, is capped at 50 rows without pagination, and the UI status map has REJECTED (no route sets it) but no CANCELLED [CODE-VERIFIED]; (26) expense handlers return error.message in 500 bodies (:123, :162, :170, :181, :223) [CODE-VERIFIED]; (27) DECISION PENDING (owner): the backend has no expense state machine, so the Item 3 UI gates buttons by status but that is convenience, not enforcement; backend hardening (status preconditions via conditional updates, transactional pay, account selection by type, amount validation, loan status derivation, adjustment approve guard, vendor PUT whitelist, malformed-JSON 400, error.message removal, limiter keying) is queued as a separate gated batch; (28) expenses state transitions [OBSERVED live 2026-09-20, throwaway tenant]: PATCH pay on a PENDING expense returned 200 and set PAID (approval skipped); PATCH approve on a PAID expense returned 200 and set APPROVED; a CANCELLED expense went CANCELLED to APPROVED to PAID with 200 each time; approve, DELETE and PUT on a nonexistent id returned 200 and pay returned 404; (29) expenses second pay [OBSERVED live]: PATCH pay on an APPROVED expense that already had a PAY journal entry returned 500 (unique constraint on tenant_id and entry_number, entry number is PAY- plus the expense number) after the status had already been rewritten to PAID, which proves the non-atomic pay of (22); the 500 body contains Prisma text; (30) expenses edits after payment [OBSERVED live]: PUT amount 999 on a PAID expense returned 200 and changed the amount from 100.00 to 999.00 while the journal entry stayed 100.00; DELETE on that PAID expense set CANCELLED with the journal entry untouched; PUT amount -5 was stored as -5.00; PUT amount abc or an object returned 500 with Prisma text that includes the tenant id; (31) expenses pay account [OBSERVED live]: the category UI3 Cat matched no account name, so the fallback (first EXPENSE account, no ORDER BY) picked 5000 Cost of Goods Sold and both pay journals debited COGS and credited 1000 Cash on Hand: any expense whose category name is not a substring of an account name is booked to COGS; a category name that matches a non-expense account was not tested; (32) default role permissions in a NEW tenant [OBSERVED live, throwaway tenant, printed after the fixture step]: manager holds expenses.approve and expenses.pay but no expenses.view, so GET /expenses (guarded by expenses.view) should return 403 for a manager and the Item 3 buttons could not be used by that role [inferred from the route guard, not tested]; admin holds all seven expense permissions; approve and pay are not separated by default; the same matrix on tenant 1 is NOT yet checked; (33) deploy-sitara.ps1 failed at the git pull step when run with 2>&1 redirection [OBSERVED live 2026-09-20], see Tooling rules; (34) default roles on tenant 1 [OBSERVED live 2026-09-20, read-only query]: accountant holds expenses.approve, create, pay and view (no update or delete); manager holds expenses.approve and expenses.pay plus purchases.create, receive, returns, update and view but NO expenses.view; admin holds all seven expense slugs. Consequences: the accountant can create, approve and pay its own expense (Item 3 shows Approve and Pay to that role); GET /expenses is guarded by expenses.view (expense.routes.ts:87), so a manager should get 403 there and the expenses page shows an empty list because load ignores errors (expenses/page.tsx:33-34) [inferred, live probe pending]; (35) purchase returns are created already APPROVED with every effect applied [CODE-VERIFIED]: POST /purchases/returns (purchase.routes.ts:260-358) sets status APPROVED (:291) and in one transaction decrements warehouse stock (:307-310), decrements one FIFO batch (:313-319), writes a PURCHASE_RETURN movement (:321-323), posts Dr Accounts Payable / Cr Inventory (:326-343) and reduces the vendor balance with a ledger row (:346-350); PATCH /purchases/returns/:id/approve (:360-369) is a bare updateMany status stamp with no state filter and no effects, returns 200 for a nonexistent id, and uses the same slug purchases.returns as create; approvedBy stays null on every created return, so any purchases.returns holder can later stamp approvedBy and approvedAt; there is never a PENDING return via the API, so an Approve button would have nothing to act on; (36) purchase return create integrity [CODE-VERIFIED; live probe pending]: the stock decrement is silently skipped when stock is lower than the quantity (:308) while the journal and vendor balance still change; the FIFO decrement has no floor (:317-318); quantity and unitCost come from the client with no cap against received or purchased quantity and no receipt reference; a negative unitCost makes totalAmount negative, the journal is skipped (:327) but the vendor balance still moves (:347); journal entries are skipped silently if the AP or Inventory account is missing (:332); returnNumber uses 4 random digits under a unique constraint; notes is destructured but never stored; 500 bodies include error.message (:357, :368); (37) purchases/returns/create/page.tsx hardcodes warehouseId 1 (:40) while the API checks warehouse ownership per tenant (:277-278), so a tenant that does not own warehouse id 1 should get 403 on every create (a new tenant default warehouse was observed with id 62 on 2026-09-20); the receipt picker sends no receiptId and does not filter by vendor (:24), the quantity cap is client-side only (:65), the list page (purchases/returns/page.tsx) has no approve affordance, no pagination controls although the API pages by 20, and an ungated New Return button [CODE-VERIFIED; live probe pending]; (38) DECISION PENDING (owner): Item 4 is not built because an approve UI would be dead code; the real fix is a backend redesign (create as PENDING, apply effects on approve in one transaction with state guards and a separate approve slug) and belongs to the hardening batch; a frontend-only fix for the hardcoded warehouse is possible; (39) purchase return probes [OBSERVED live 2026-09-21, throwaway tenant, admin role; supersedes the live probe pending tags in (36) and (37)]: POST /purchases/returns with 10 units at cost 60 returned 201 and moved stock 100 to 90, vendor balance 0.00 to -600.00 and posted one journal of 600.00; the body the create page sends (warehouseId 1) returned 403 Warehouse 1 not found or not accessible, because the new tenant warehouse had id 67 (on tenant 1 warehouse 1 exists), so the create page can only work for tenant 1; quantity 500 with 90 on hand returned 201, stock stayed 90 (decrement silently skipped) while vendor balance moved to -5600.00 and a second journal of 5000.00 was posted; unitCost 1000000 returned 201 (vendor balance -1005600.00, no cap); unitCost -50 returned 201, stock was still decremented by 1, vendor balance moved UP by 50 to -1005550.00 and no journal was posted; missing productId returned 500 with the text Cannot convert undefined to a BigInt; nonexistent vendor returned 404; PATCH approve returned 200 twice on the same return and 200 for id 999999999 with stock, vendor balance and journal unchanged, and 400 for abc; the status and approvedBy values were NOT observed because the probe queried snake_case column names while the purchase_returns table uses camelCase columns (totalAmount, approvedBy), so APPROVED at create stays CODE-VERIFIED (purchase.routes.ts:291); the manager role holds the same purchases.returns slug, so it can reduce a vendor payable and the inventory value by an arbitrary amount with no receipt reference (observed with the admin role); (40) manager without expenses.view [OBSERVED live 2026-09-21, new tenant default manager role]: GET /expenses returned 403 Missing required permissions: expenses.view while GET /purchases/returns returned 200; tenant 1 manager has the same expense slugs (read-only query), so that role cannot use the expenses page (the page load ignores errors and would show an empty list); supersedes the inferred tags in (32) and (34); (41) product import UI [CODE-VERIFIED, products/page.tsx, 174 lines]: import is a hand-made fixed-position modal (not the Dialog component) at :136-171, opened from the Import button at :115 (products.import), with a raw fetch to /api/v1/products/import at :152 that bypasses lib/api (no token refresh); the response is stored as data.data or data (:154) and only an error key is treated as failure (:159), so an error body of the shape status and detail (as seen elsewhere in this project) would render three zero cards (Imported 0, Skipped 0, Total 0) as if the import succeeded [inferred, not observed]; no import history UI exists anywhere in the web app; (42) product import backend [CODE-VERIFIED, product.routes.ts:215-268]: rows are created one at a time with no transaction inside Promise.all batches of 100 (:223-246); the category and SKU checks are find-then-create with no lock (:230, :234-235), so rows of one file that share a new category or SKU can race [probed in the Item 5 round]; the history row is written after the loop (:249-253) and stores only counts (errors is an Int count, status defaults to COMPLETED, no failure status); GET /products/import/history (:258-268) is guarded by products.import, filtered by tenant and entityType products, ordered createdAt desc, take 20, and turns any error into an empty list; (43) product import races [OBSERVED live 2026-09-21, throwaway tenant, admin role]: a 3-row file whose rows share one NEW category returned 200 with imported 1, skipped 2 and per-row errors whose reason is the raw Prisma text of a unique-constraint failure on (tenant_id, name) in productCategory.create; only the first row was created and exactly one category exists, so valid rows are lost whenever several rows of a batch share a category that does not exist yet (the find-then-create at product.routes.ts:234-235 races inside Promise.all); (44) a file with the same SKU twice returned 200 with imported 1, skipped 1 and a Prisma unique-constraint text on (tenant_id, sku); exactly one product was created (the database constraint holds, the message is raw Prisma text); (45) import edge responses [OBSERVED live 2026-09-21]: no file gives 400 with status and detail CSV file required; a header-only file gives 200 with all-zero counts AND writes a history row; a 6 MB file gives 400 with type, title, detail (File too large, max 5MB) and status and writes no history row; every history row observed has status COMPLETED, even race.csv where 2 of 3 rows failed, so the status column carries no information; the history list of a second tenant contained only its own three files (isolation observed); (46) the import modal (products/page.tsx:148-165) treats only an error key as failure, and the 400 bodies above have detail but no error key, so a rejected upload should render Imported 0, Skipped 0, Total 0 [bodies OBSERVED; rendering OBSERVED live 2026-09-21: a rejected 6 MB upload posted once, returned 400 and the modal rendered Imported 0, Skipped 0, Total 0 with Close and Import Another, i.e. a rejection looks like an empty success]; (47) modal overlay stacking [OBSERVED live 2026-09-21, ui5b_admin.js, History modal open at 1280x800, elementFromPoint]: the top-left point hit the sidebar header div, the top-right point hit the sticky page header, both bottom corners hit the modal overlay, and a click at the bottom-right corner closed the modal; so the hand-made fixed inset-0 z-50 overlay used by the import and history modals does not cover the sidebar or the header (cause not confirmed, a stacking context around the page content is suspected) [INFERRED]; (48) return processing logs [OBSERVED live 2026-09-21, read-only]: return_processing_logs holds 0 rows on every tenant; tenant 1 return-related slugs: admin holds pos.returns.create, pos.returns.process, purchases.returns, sales.returns.approve, sales.returns.create and sales.returns.view; manager holds purchases.returns, sales.returns.approve, sales.returns.create and sales.returns.view; cashier holds pos.returns.create and pos.returns.process only; accountant holds none; (49) [CODE-VERIFIED] the only writer of return_processing_logs is POST /pos/process-return (pos.routes.ts:364-369), executed after the 201 response inside a swallowed try, always with status SUCCESS and a null errorMessage; a failure (pos.routes.ts:370) returns 500 and writes no row; sales-returns.routes.ts writes no log; therefore GET /sales/return-logs/failed and stats.failed can never be non-zero with the current code, the standalone sales-return flow is not logged at all, and cartData is stored but never returned by the API; (50) [CODE-VERIFIED, restates (8)] return-logs.routes.ts:9,21,33 carry no rbac although rbacMiddleware is imported (:4) and the mount (routes/index.ts:46) adds only authMiddleware and tenantMiddleware, so any signed-in user of a tenant can read that tenant log; Item 6 is built UI-only and this exposure stays open unless the owner authorizes the three-line backend gate; (51) DataTable renders five empty skeleton rows while loading (data-table.tsx:116-123), so a test that counts rows or reads the first cell must wait for real row text; the first Item 6 run (17/19, 2026-09-21) failed on exactly that: order and stats were read before the data rendered and a five-rows check passed vacuously on the skeleton rows; the backend answered correctly (/stats returned total 5, success 3, failed 2) [OBSERVED live]; (52) return log rows are written only by POST /pos/process-return, which the POS page calls for standalone returns (pos/page.tsx:374; body customerId, items with saleItemId, quantity and unitPrice, reason, refundMethod, notes; 400 Items required) [CODE-VERIFIED]; real-row probe (throwaway tenant, admin): RESULT-LOG rows=0 statuses=none return-code=403; failing-return probe (nonexistent saleItemId): RESULT-FAIL rows-after-failing-return=0 failing-return-code=400; (53) return log read exposure [OBSERVED live, throwaway tenant, role cashier without sales.returns.view]: INFO cashier-direct-list -> 200 {"data":[{"id":"14","saleId":"105","returnNumber":"RET-260921-0005","status":"FAILED","errorMessage":"Insufficient stock for product 7","createdAt":"2026-09-21T (a signed-in user of the tenant can read that tenant log through the API; the page itself is gated by sales.returns.view); (54) POS standalone return contract mismatch [CODE-VERIFIED + OBSERVED live 2026-09-21, throwaway tenant, admin]: POST /pos/process-return rejects any item without a valid in-tenant productId (pos.routes.ts:311-313, BigInt(item.productId || 0), 403 Product undefined not found or not accessible) while the POS page sends items with only saleItemId, quantity and unitPrice (pos/page.tsx:374-380); a request in exactly that shape after a real completed sale: RESULT-UISHAPED code=403 stock=98 logs=0 journals=0/0; return_processing_logs holds 0 rows on every tenant observed, so the standalone POS return can not succeed from the UI and the Item 6 log has never had a real row; the apply-to-current-sale branch (pos/page.tsx:365-368) is a different path and was not tested; (55) POS return endpoint with a correct body [OBSERVED live 2026-09-21, throwaway tenant, admin; the endpoint trusts client quantity and unitPrice, pos.routes.ts:316]: VALID: RESULT-VALID code=201 stock=99 logs=1 journals=1/100.00; OVERQTY (5 units on a sale of 2, 1 already returned): RESULT-OVERQTY code=201 stock=104 logs=2 journals=2/600.00; PRICE (unitPrice 100000 on a price-100 item): RESULT-PRICE code=201 stock=105 logs=3 journals=3/100600.00; DOUBLE: RESULT-DOUBLE code=201 stock=107 logs=4 journals=4/100800.00; NEGQTY: RESULT-NEGQTY code=201 stock=110 logs=5 journals=4/100800.00; NOCUST: RESULT-NOCUST code=400 stock=110 logs=5 journals=4/100800.00; (56) Item 6 admin login timeout [OBSERVED 2026-09-21]: the first retest attempt hit a 30 s login timeout with no assertion executed; cause NOT established; login limiter keys read afterwards: auth_keys=0 ratelimit_keys=0 | NOT RUN; (57) DECISION PENDING (owner): fix for follow-up 54; (58) POS return picker CAN mix sales [CODE-VERIFIED, pos/page.tsx:784-795 read 2026-09-21]: customerPurchases.map iterates every sale for the customer, each sale item gets its own Add button appending into one shared returnItems array, across pagination, with no per-sale boundary and no reset except on modal-open (:326); the fix below therefore groups items by server-resolved sale rather than rejecting mixed-sale requests; (59) POS return fix landed, multi-sale-aware [OBSERVED live, throwaway tenant, admin]: pos.routes.ts process-return resolves saleItemId/productId/unitPrice server-side, rejects non-positive quantity, checks remaining quantity against sales_return_items inside the transaction (scoped per saleItemId across the whole request, closing sequential double-submit but not true concurrent races - no SaleItem row lock), groups items by real sale and creates one SalesReturn + journal + ledger entry per sale group in one transaction, and writes sales_return_items rows (previously never written - see follow-up 54). Response contract: data.returnNumber/total now reflect the first/aggregate group for backward compatibility with the frontends one read at pos/page.tsx submitReturn (only res.data.returnNumber is consumed), plus a new data.returns array with one entry per sale group. Regression: UISHAPED (no productId, the exact shape the POS page sends): RESULT-UISHAPED code=201 stock=196 returns_sale1=1 returns_sale2=0 logs=1 journals=1/100.00; MULTISALE (items from two different sales in one request): RESULT-MULTISALE code=201 stock=198 returns_sale1=2 returns_sale2=1 logs=3 journals=3/300.00; OVERQTY: RESULT-OVERQTY code=400 stock=198 returns_sale1=2 returns_sale2=1 logs=3 journals=3/300.00; SPLITOVER: RESULT-SPLITOVER code=201 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00; PRICE (client sent 999999, server used the real sale price): RESULT-PRICE code=400 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00; NEGQTY: RESULT-NEGQTY code=400 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00; NOCUST/MISSINGID/NONEXISTENTID (all must be 400 not 500): RESULT-NOCUST code=400 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00 | RESULT-MISSINGID code=400 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00 | RESULT-NONEXISTENTID code=400 stock=200 returns_sale1=3 returns_sale2=2 logs=5 journals=5/500.00; customer-purchases exclusion after the 5-minute cutoff cleared: RESULT-EXCLUSION SI1-present=0 SI2-present=0. No frontend change was needed; the existing POS payload became valid as a side effect of deriving productId and unitPrice server-side, closing follow-up 54; (60) POS return price-override defense confirmed live [OBSERVED live, throwaway tenant, admin]: a process-return request with remaining quantity available and a tampered unitPrice of 999999 was accepted (the server derives the real sale price server-side, as designed) while the journal Return entry read the real sale price, not the tampered one - closing the gap left by follow-up 59 where the prior PRICE test case was rejected on quantity before reaching price derivation. Negative case alongside: the same tampered price combined with a genuinely over-remaining quantity was still rejected 400, confirming price tampering cannot be used to bypass the quantity ceiling. PRICEOVERRIDE: RESULT-PRICEOVERRIDE code=201 stock=46 logs=1 journals=1/100.00; PRICEOVERRIDE_OVERQTY: RESULT-PRICEOVERRIDE_OVERQTY code=400 stock=46 logs=1 journals=1/100.00 (option A: the POS page sends productId, UI-only; option B: the server derives productId from the sale item, backend change); no code changed; (61) DECISION PENDING (owner): follow-up 27 (expense state machine) re-confirmed against current code this round - see STEP C1 evidence in this session for exact line references, no drift from follow-ups 21-26; (62) DECISION PENDING (owner): follow-up 38 (purchase return architecture) re-confirmed against current code this round - see STEP C2 evidence in this session for exact line references, no drift from follow-ups 35-37; (63) loans cross-tenant PUT [OBSERVED live, two throwaway tenants]: RESULT-CROSSPUT RESULT-CROSSPUT {"data":{"message":"Loan updated"}} code=200; RESULT-DBNOTES RESULT-DBNOTES notes=[]; RESULT-CROSSGET RESULT-CROSSGET {"status":404} code=404 - closes the not-verified tag on follow-up 23 (loans Item 2 note); (64) expenses category prefill and manager gating [OBSERVED live, throwaway tenant]: single-expense GET categoryId presence RESULT-GETONE {"type":"https://httpstatuses.io/404","title":"Not Found","detail":"Route GET /api/v1/expenses/20 not found","status":404}; manager (no expenses.view) list/object/approve behavior: NOT RUN | NOT RUN | NOT RUN - closes the not-verified tags on follow-up 130 and follow-up 64 manager tail; (65) expenses manager-without-view object-level gating [OBSERVED live, throwaway tenant]: RESULT-MGRLIST RESULT-MGRLIST {"type":"https://httpstatuses.io/403","title":"Forbidden","detail":"Missing required permissions: expenses.view","status":403} code=403; RESULT-MGRAPPROVE RESULT-MGRAPPROVE {"data":{"message":"Expense approved"}} code=200; RESULT-MGRPAY RESULT-MGRPAY {"data":{"message":"Expense paid"}} code=200; RESULT-FINALSTATUS RESULT-FINALSTATUS status=[PAID] - this closes the last open item from the 123/130 investigation arc; permission-slug gating on approve/pay is independent of the expenses.view gate on the list route (UI convenience vs actual RBAC enforcement, confirmed as two separate axes; (66) DECISION PENDING (owner): purchase-return redesign scoped; (67) purchase-return redesign LANDED [code bca5f8f, deployed, HEALTH_OK]: create-as-PENDING with receipt-linked server-derived items, effects-only-on-approve in one transaction with state guards, split slugs purchases.returns.create/approve (Manager keeps create only), FIFO decrement with floor loop, REJECT:400 catch boundary; schema migration 20260923010000 (receipt_item_id required, Restrict, index) applied to prod via migrate deploy; tenant-1 backfill granted create to Admin+Manager and approve to Admin only; (68) redesign regression [OBSERVED live, throwaway tenant, admin]: CREATE valid: RESULT-CREATE code=201 status=PENDING stock=10 vb=600.00 journals=0|0; OVERCAP: RESULT-OVERCAP code=400; APPROVE: RESULT-APPROVE code=200 status=APPROVED stock=8 vb=480.00 journals=1|120.00; REAPPROVE: RESULT-REAPPROVE code=400; REJECT: RESULT-REJECT code=200 status=REJECTED stock=8 vb=480.00 journals=1|120.00; MGR (create-only role): RESULT-MGR code-create=201 code-approve=403; FLOOR (stock-floor live test): RESULT-FLOOR code=401 status= stock=8 - INVALID, the second login in that script invalidated the first admin token (401 Session is no longer valid on the old token) so the floor approve never executed; the floor REJECT path stays CODE-VERIFIED from the landed diff, NOT observed live [CODE-VERIFIED + OBSERVED live read-only, 2026-09-23]: schema, caller, and data-shape investigation completed this round for the follow-up 38 redesign (create-as-PENDING, effects-on-approve, separate approve slug, state guards); see this session STEP D1-D4 evidence for exact findings before implementation begins)

### Moderate tier Item 2: loan notes edit dialog (commit 53e3666, deployed)
- loans/[id]/page.tsx only (+21/-4): Edit button gated on loans.manage, dialog with ONE field (notes), PUT sends exactly {"notes": ...}, read-only Notes line added. status is deliberately NOT editable (the backend derives it on every payment; a status select would expose the free-string bypass). Rollback = revert 53e3666. No backend change.
- Browser evidence 2026-09-20 (Playwright, D:\SasS-uitest\ui2_admin.js and ui2_viewer.js, throwaway tenant): admin PASSED 18/18 (one PUT -> 200 with body notes only, toast, persisted after reload, Escape adds no PUT); view-only user PASSED 6/6 (heading visible as positive control, Edit absent, direct PUT -> 403 Missing required permissions: loans.manage).
- Not verified: cross-tenant PUT on loans (same updateMany tenant scoping as vendors, not re-tested).
- Backend probes on a throwaway tenant 2026-09-20 [OBSERVED live]: PUT {"status":"PAID"} on an ACTIVE loan with no payments stored PAID with remaining 1000.00; the next payment recomputed status to ACTIVE; PUT {"status":"GARBAGE"} stored the free string; PUT with principalAmount, remainingAmount, totalPaid, tenantId and loanNumber returned 200 but changed none of them (immutable-column md5 identical); PUT /loans/abc returned 400 and PUT /loans/999999999 returned 200; PUT with an object-valued notes or status returned 500 with the full Prisma error text in the body; a payment of 5000 against remaining 900 was accepted (status PAID, remaining 0.00, total_paid 5100.00) and both journal entries were booked at the full paid amount (100 and 5000, each balanced).

### Moderate tier Item 3: expense row actions (commit 552bbbb, deployed)
- expenses/page.tsx only (+23/-11): actions column with Approve (PENDING, expenses.approve), Pay (APPROVED, expenses.pay, confirm dialog), Edit (PENDING, expenses.update, reuses the create dialog, sends only description, amount, categoryId, expenseDate) and Cancel (PENDING or APPROVED, expenses.delete, confirm dialog); no Reject button because no route sets REJECTED. The gating is a convenience only: the backend has no state machine (follow-ups 21 and 28-31). Rollback = revert 552bbbb. No backend change.
- Browser evidence 2026-09-20 (Playwright, D:\SasS-uitest\ui3_admin.js and ui3_viewer.js, throwaway tenant): admin PASSED 34/34 (buttons per state; approve then pay with one PATCH each and a confirm dialog; edit prefill and PUT body keys; cancel dismissed sends no DELETE, accepted sends one; new-expense dialog reset; states persist after reload); a user without update, delete, approve and pay PASSED 7/7 (row visible, 0 action buttons, four direct calls each 403 with the matching slug).
- Deploy: deploy-sitara.ps1 run with 2>&1 failed at the git pull step; the pull itself had succeeded, and build, restart and health gate were then run as a hand-made script (HEALTH_OK, VPS HEAD 552bbbb). The browser run against the deployed site shows the new code is live.
- Not verified: per-permission positive gating (a user holding only approve or only pay); the category select prefill in the edit dialog (the save succeeded, so categoryId was non-empty); cross-tenant behavior; a user without expenses.view.

### Moderate tier Item 4: purchase-return approve UI - NOT BUILT (premise invalid)
- Read-only investigation 2026-09-20 (purchase.routes.ts 232-371, schema 906-953, purchases/returns pages): purchase returns are created already APPROVED and all stock, journal and vendor-balance effects are applied at create; PATCH approve only stamps a status. An Approve button (PENDING rows only) would never render, so no UI was built. See follow-ups 34-38.
- Live probes 2026-09-21 (throwaway tenant): see follow-ups 39 and 40; approving changes nothing, and create applies stock, journal and vendor balance immediately with client-supplied quantities and unit costs.

### Moderate tier Item 5: product import history modal (commit f0467c9, deployed; browser evidence complete: see the last bullet)
- products/page.tsx only (+12/-2): a History button (gated on products.import) opens a hand-made modal like the import modal, loaded through apiGet from GET /products/import/history (20 newest rows: file, date, total, imported, skipped, errors, status), with empty and load-error states. Cosmetic: the line async function load() lost its indentation in the edit. Rollback = revert f0467c9. No backend change.
- Browser evidence 2026-09-21 (Playwright ui5_admin.js, throwaway tenant): PASSED 22/24: empty state with one GET 200; two real CSV imports through the UI (cards 3/0/3 and 2/2/4, response bodies as expected); history newest first with correct counts, date cell and COMPLETED; persistence after reload; no page errors, no 429. The 2 FAILs are test-design errors, not confirmed app defects: the expected error text used a hyphen while the live text has an em dash, and a click at viewport (5,5) did not close the modal (a sidebar or other element may cover that corner; not yet diagnosed). Viewer gating (U2) was not run.
- Browser evidence completed 2026-09-21 (Playwright, throwaway tenant, seeded through the real import endpoint): ui5b_admin.js PASSED 18/18 (seeded two-row history newest first with headers, counts, dates and one GET 200; mocked 500 and mocked empty states render and close; a 6 MB upload posts once, returns 400 and renders three zero cards; no page errors, no 429) and ui5b_viewer.js PASSED 8/8 (manager without products.import sees products but no History or Import button, direct GET history and POST import return 403 naming products.import, product list 200). Item 5 is closed on behavior. Follow-ups 43-47 record the import defects found.

### Moderate tier Item 6: return processing log viewer (commit 0419921, deployed)
- New page /sales/return-logs (57 lines) plus one sidebar entry under Sales (Return Logs, gated on sales.returns.view): stat cards (total, successful, failed from GET /sales/return-logs/stats), All and Failed tabs (GET /sales/return-logs and /failed, latest 50), empty, error and no-access states, read-only. No backend change; the log routes still have no permission check (follow-ups 50 and 53). Rollback = revert 0419921.
- Evidence 2026-09-21 (Playwright, throwaway tenants; log rows were SEEDED by SQL because the app has never written a FAILED row): first run ui6_admin.js 17/19 (the two FAILs were timing artifacts, follow-up 51); retest ui6b_admin.js SCRIPT-FAILED at login in the first attempt (no assertion ran; Item 6 admin rendering was not observed with real rows), second attempt 2026-09-21: PASSED 24/24; cashier test ui6b_cashier.js PASSED 8/8.
- The Failed tab cannot show a real failure with the current code: the only writer records SUCCESS after the response (follow-up 49).

### Moderate tier orphan audit: SUMMARY (2026-09-20 to 2026-09-21)
- Item 1 vendor edit page 1e4d8ca (plus Item 1.6 Return link e1088ed and mount-only effect f4847f2); Item 2 loan notes dialog 53e3666; Item 3 expense row actions 552bbbb; Item 4 purchase-return approve NOT BUILT (returns are created APPROVED, approve is a no-op); Item 5 product import history modal f0467c9; Item 6 return log viewer 0419921 (cashier gating 8/8; admin real-data rendering: PASSED 24/24; the viewer has never displayed a real log row because of follow-up 54). All UI-only, no backend change.
- Everything behind these screens is unhardened (follow-ups 1, 2, 10, 13-15, 21-24, 28-31, 35-37, 39, 43, 50, 53). Backend hardening is a separate gated batch (follow-ups 27 and 38) and needs the owner's explicit go-ahead per item; the limiter issue (follow-ups 3 and 4) must also be settled before go-live.

### Modules Built (prior sessions):
- POS checkout: FIFO batch consumption, balanced journal entries, dynamic warehouse lookup
- Sales returns: embedded returns now visible in menu, `EMBEDDED`/`STANDALONE` type badges
- Fixed ~112 hardcoded values (P0 items: createdBy, warehouseId, account codes)
- Reverted test sales 17, 33; patched unbalanced JE for F52108
- Seeded Zara Bags & Accessories demo data (20 products, 12 sales, 3 POs)
- Created AGENTS.md / README.md / SESSION_SUMMARY.md

### Files Changed This Session:
- `apps/api/src/routes/pos.routes.ts`
- `apps/api/src/routes/sales-returns.routes.ts`
- `apps/api/src/constants/accounts.ts`
- `apps/api/src/utils/warehouse.ts` (new)
- `apps/web/src/app/(dashboard)/sales/returns/page.tsx`

### Known P0 Issues (not yet fixed):
1. Normal sales (no return items) do NOT create journal entries — all sales should
2. Reports module: all 13 report endpoints return stubs with zeros
3. Purchase receiving: no stock update, no auto-journal
4. RBAC enforcement: only applied to product and customer mutations; most routes unprotected

---

## DO NOT

- Do NOT hardcode IDs (warehouseId=1, createdBy=1, productId=1, accountId=13)
- Do NOT use account code strings like `'4100'` directly — use `ACCOUNT_CODES.SALES_RETURNS`
- Do NOT create a revert script without a `DRY_RUN=true` mode
- Do NOT commit a revert script — delete it after execution
- Do NOT modify files outside `apps/api/src/` or `apps/web/src/` unless necessary for configuration
- Do NOT modify `config/index.ts` fallback credentials — they are `.env` fallbacks for development
- Do NOT create new tenants during development — reuse the `demo` tenant (slug: `demo`)

---

## Common File Locations

| What | Where |
|------|-------|
| Database schema | `apps/api/prisma/schema.prisma` |
| Route registration | `apps/api/src/routes/index.ts` |
| Account code constants | `apps/api/src/constants/accounts.ts` |
| Warehouse lookup helper | `apps/api/src/utils/warehouse.ts` |
| JWT helpers | `apps/api/src/utils/helpers.ts` |
| Prisma client + tenant extension | `apps/api/src/lib/prisma.ts` |
| Auth middleware (JWT) | `apps/api/src/middleware/auth.ts` |
| RBAC middleware | `apps/api/src/middleware/rbac.ts` |
| Tenant middleware | `apps/api/src/middleware/tenant.ts` |
| Frontend API client | `apps/web/src/lib/api.ts` |
| Frontend auth context | `apps/web/src/lib/auth.tsx` |
| Dashboard layout + sidebar | `apps/web/src/app/(dashboard)/layout.tsx` |
| Reusable DataTable | `apps/web/src/components/data-table.tsx` |

---

## Next Priority (for the next session)

| Priority | Module | Task | File |
|----------|--------|------|------|
| P0 | POS/Checkout | Add journal entry creation for normal sales (no returns) | `pos.routes.ts` |
| P0 | Purchases | Purchase receiving with stock update + auto-journal | `purchase.routes.ts` |
| P0 | Reports | Sales/Inventory/Purchase reports with real data | `report.routes.ts` |
| P0 | RBAC | Route-level permission enforcement across all modules | All route files |
| P1 | POS | Multi-warehouse selection in checkout | `pos.routes.ts`, `pos/page.tsx` |
| P1 | POS | Pricing tiers with discount | `pos.routes.ts` |
| P1 | Sales | Cancel sale with full reversal | `sale.routes.ts` |
| P1 | Settings | Persist settings to database | `settings.routes.ts` |

## Tooling rules (Windows PowerShell 5.1 + ssh harness; learned the hard way)
- PowerShell 5.1: no &&, no head/tail/grep; curl is Invoke-WebRequest (use curl.exe); quote paths containing parentheses.
- Remote bash only as a script file sent with the base64 pattern; never inline bash inside a PowerShell string.
- Never write into /root/sitara. Expected untracked VPS files: .env.bak-jwtrotate, apps/web/.env.production, deploy.sh, ecosystem.config.js, uploads/.
- Web has no typecheck script: run npx tsc --noEmit inside apps/web (and apps/api). Never run a local next build (it hangs); the VPS build in deploy-sitara.ps1 is the build gate.
- Never git add -A or git add .; add files by explicit path. Diff before commit.
- Do not use the Edit tool on AGENTS.md (non-ASCII anchors fail); use a .ps1 with UTF-8 no BOM.
- Write script files UTF-8 no BOM and check the first 3 bytes.
- Auth limiter = 5 logins per 15 min per Cloudflare edge address; general limiter = 30 req/min: log in once per test run, reuse the session, space navigations 25 s apart.
- Playwright: isVisible() does not wait; use waitFor/expect-style waits before asserting content that loads asynchronously.
- Reports: opening checklist DONE/NOT-RUN per step ID, then OBSERVED-LIVE / CODE-VERIFIED / NOT-VERIFIED / NOT-RUN. Never write closed/tested/passed without pasted output; never invent hashes.
- Deploy: run .\deploy\vps\deploy-sitara.ps1 plainly (no 2>&1, no pipe, no redirect): under PowerShell 5.1 redirected stderr turns git output into a terminating NativeCommandError. Start-Transcript does not persist between tool calls in this harness (each call is a fresh PowerShell host, observed 2026-09-21); the deploy output is shown in the tool output itself, so do not use it. If it fails, STOP and report; do not improvise a replacement deploy.
